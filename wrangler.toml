# Cloudflare Pages Configuration
# For manual setup in Cloudflare Dashboard:
#   - Build command: npm run build
#   - Output directory: dist
#   - Root of output is the `dist` directory (served as static site)
#
# The dist/wrangler.json is auto-generated by @cloudflare/vite-plugin
# during `npm run build`. Do NOT edit dist/wrangler.json directly —
# edit wrangler.toml or vite.config.js instead.

name = "what-to-learn"
compatibility_date = "2026-04-30"
pages_build_output_dir = "dist"

# Cloudflare Pages: connect your GitHub repo at:
# https://dash.cloudflare.com -> Pages -> Create a project -> Connect to Git
